/*
 * Codebeamer swagger API
 * No description provided (generated by Swagger Codegen https://github.com/swagger-api/swagger-codegen)
 *
 * OpenAPI spec version: 3.0
 *
 * NOTE: This class is auto generated by the swagger code generator program.
 * https://github.com/swagger-api/swagger-codegen.git
 *
 * Swagger Codegen version: 3.0.61
 *
 * Do not edit the class manually.
 *
 */
import ApiClient from '../ApiClient';
import ReportReferenceLevelSettings from './ReportReferenceLevelSettings';
import ResizableReportColumnSettings from './ResizableReportColumnSettings';
import TrackerReference from './TrackerReference';

/**
 * The TrackerReportSettings model module.
 * @module model/TrackerReportSettings
 * @version 3.0
 */
export default class TrackerReportSettings {
  /**
   * Constructs a new <code>TrackerReportSettings</code>.
   * Settings of a report on a tracker.
   * @alias module:model/TrackerReportSettings
   * @class
   * @param cbQl {String} CbQL query string of the report.
   * @param columns {Array.<module:model/ResizableReportColumnSettings>} Column definitions.
   * @param description {String} Description of the report.
   * @param isPublic {Boolean} Public report indicator.
   * @param name {String} Name of the report.
   * @param tracker {module:model/TrackerReference} 
   */
  constructor(cbQl, columns, description, isPublic, name, tracker) {
    this.cbQl = cbQl;
    this.columns = columns;
    this.description = description;
    this.isPublic = isPublic;
    this.name = name;
    this.tracker = tracker;
  }

  /**
   * Constructs a <code>TrackerReportSettings</code> from a plain JavaScript object, optionally creating a new instance.
   * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
   * @param {Object} data The plain JavaScript object bearing properties of interest.
   * @param {module:model/TrackerReportSettings} obj Optional instance to populate.
   * @return {module:model/TrackerReportSettings} The populated <code>TrackerReportSettings</code> instance.
   */
  static constructFromObject(data, obj) {
    if (data) {
      obj = obj || new TrackerReportSettings();
      if (data.hasOwnProperty('cbQl'))
        obj.cbQl = ApiClient.convertToType(data['cbQl'], 'String');
      if (data.hasOwnProperty('columns'))
        obj.columns = ApiClient.convertToType(data['columns'], [ResizableReportColumnSettings]);
      if (data.hasOwnProperty('description'))
        obj.description = ApiClient.convertToType(data['description'], 'String');
      if (data.hasOwnProperty('isPublic'))
        obj.isPublic = ApiClient.convertToType(data['isPublic'], 'Boolean');
      if (data.hasOwnProperty('name'))
        obj.name = ApiClient.convertToType(data['name'], 'String');
      if (data.hasOwnProperty('referenceLevelSettings'))
        obj.referenceLevelSettings = ApiClient.convertToType(data['referenceLevelSettings'], [ReportReferenceLevelSettings]);
      if (data.hasOwnProperty('renderingMethod'))
        obj.renderingMethod = ApiClient.convertToType(data['renderingMethod'], 'String');
      if (data.hasOwnProperty('reportId'))
        obj.reportId = ApiClient.convertToType(data['reportId'], 'Number');
      if (data.hasOwnProperty('showAllChildren'))
        obj.showAllChildren = ApiClient.convertToType(data['showAllChildren'], 'Boolean');
      if (data.hasOwnProperty('showAncestors'))
        obj.showAncestors = ApiClient.convertToType(data['showAncestors'], 'Boolean');
      if (data.hasOwnProperty('showDescendants'))
        obj.showDescendants = ApiClient.convertToType(data['showDescendants'], 'Boolean');
      if (data.hasOwnProperty('tracker'))
        obj.tracker = TrackerReference.constructFromObject(data['tracker']);
    }
    return obj;
  }
}

/**
 * CbQL query string of the report.
 * @member {String} cbQl
 */
TrackerReportSettings.prototype.cbQl = undefined;

/**
 * Column definitions.
 * @member {Array.<module:model/ResizableReportColumnSettings>} columns
 */
TrackerReportSettings.prototype.columns = undefined;

/**
 * Description of the report.
 * @member {String} description
 */
TrackerReportSettings.prototype.description = undefined;

/**
 * Public report indicator.
 * @member {Boolean} isPublic
 */
TrackerReportSettings.prototype.isPublic = undefined;

/**
 * Name of the report.
 * @member {String} name
 */
TrackerReportSettings.prototype.name = undefined;

/**
 * Reference level setting for Intelligent Table View.
 * @member {Array.<module:model/ReportReferenceLevelSettings>} referenceLevelSettings
 */
TrackerReportSettings.prototype.referenceLevelSettings = undefined;

/**
 * Allowed values for the <code>renderingMethod</code> property.
 * @enum {String}
 * @readonly
 */
TrackerReportSettings.RenderingMethodEnum = {
  /**
   * value: "disabled"
   * @const
   */
  disabled: "disabled",

  /**
   * value: "table"
   * @const
   */
  table: "table",

  /**
   * value: "tree"
   * @const
   */
  tree: "tree"
};
/**
 * Rendering method for Intelligent Table View.
 * @member {module:model/TrackerReportSettings.RenderingMethodEnum} renderingMethod
 */
TrackerReportSettings.prototype.renderingMethod = undefined;

/**
 * Id of a report
 * @member {Number} reportId
 */
TrackerReportSettings.prototype.reportId = undefined;

/**
 * Indicator to ability to collapse/expand all child items.
 * @member {Boolean} showAllChildren
 */
TrackerReportSettings.prototype.showAllChildren = undefined;

/**
 * Indicator to show the ancestors of a result item.
 * @member {Boolean} showAncestors
 */
TrackerReportSettings.prototype.showAncestors = undefined;

/**
 * Indicator to show the descendants of a result item.
 * @member {Boolean} showDescendants
 */
TrackerReportSettings.prototype.showDescendants = undefined;

/**
 * @member {module:model/TrackerReference} tracker
 */
TrackerReportSettings.prototype.tracker = undefined;

